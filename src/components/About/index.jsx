import React from 'react'
import { AboutSection } from './styles'

const About = ({ id }) => {
    return (
        <AboutSection id={id}>
            <h1>About</h1>
        </AboutSection>
    )
}

export default About